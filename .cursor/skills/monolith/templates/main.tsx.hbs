import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App';
{{#if dsStyleImport}}
import '{{dsStyleImport}}';
{{/if}}

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
