---
runId: {{runId}}
generated: {{generatedAt}}
status: {{status}}
owner: orchestrator
---

# Delivery — {{briefShort}}

## Run summary

- **Brief.** {{brief}}
- **DS.** {{dsName}}@{{dsVersion}} via {{dsSource}}
- **Guidelines.** {{guidelinesSource}} ({{guidelinesProvided}} provided / {{guidelinesInferred}} inferred)
- **Duration.** {{duration}}

## Artifact map

| Kind | Path |
|---|---|
| Input manifest | `out/{{runId}}/input-manifest.json` |
| DS knowledge | `out/{{runId}}/ds-knowledge/` |
| Guidelines | `out/{{runId}}/guidelines/` |
| Research | `out/{{runId}}/docs/research.md` |
| PRD | `out/{{runId}}/docs/prd.md` |
| IA | `out/{{runId}}/docs/information_architecture.md` |
| User flow | `out/{{runId}}/docs/user_flow.md` |
| Design decisions | `out/{{runId}}/docs/design_decisions.md` |
| Best practices | `out/{{runId}}/docs/best_practices.md` |
| Build specs | `out/{{runId}}/docs/build_specs.md` |
| Pattern decisions | `out/{{runId}}/docs/pattern_decisions.md` |
| QA (consolidated) | `out/{{runId}}/docs/qa.md` |
| App | `out/{{runId}}/app/` |
| Writes log | `out/{{runId}}/writes.log` |

## Localhost

```
cd out/{{runId}}/app
npm run dev       # serves at http://localhost:{{port}}
```

## Blockers

{{#if blockers}}
{{#each blockers}}
- **{{this.kind}}** ({{this.stage}}) — {{this.message}}
{{/each}}
{{else}}
None.
{{/if}}

## Warnings

{{#if warnings}}
{{#each warnings}}
- {{this}}
{{/each}}
{{else}}
None.
{{/if}}

## Patterns promoted

{{#if promotions}}
{{#each promotions}}
- [`{{this.slug}}`](../../../patterns/{{this.slug}}.md)
{{/each}}
{{else}}
None.
{{/if}}

## Phase 2 handoff

```
/rewire-to-ds <figma-frame-url>
  --plan   out/{{runId}}/docs/screen-plan.json
  --tokens out/{{runId}}/ds-knowledge/tokens.json
  --index  out/{{runId}}/ds-knowledge/component-index.json
```

(Phase 1.5 — html.to.design import — is manual. Point it at http://localhost:{{port}}/{{primaryRoute}}.)
