// Theme provider — adapter-pattern.
// The exact implementation depends on the DS's themeAccess style:
//   - theme-object : wrap in the DS's <Provider theme={...} /> (hook access)
//   - css-vars     : attach :root / data-theme CSS custom properties
//   - classnames   : apply a theme class on <html> or <body>
//   - tailwind     : configure Tailwind preset + class strategy
// The scaffolder picks the right branch from the adapter + resolved guidelines.

import React from 'react';
{{#if dsProviderImport}}
{{dsProviderImport}}
{{/if}}

{{#if themeConfig}}
const themeConfig = {{{themeConfig}}};
{{/if}}

export function ThemeProvider({ children }: { children: React.ReactNode }) {
  {{{providerBody}}}
}
