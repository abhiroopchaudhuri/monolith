// Custom component: {{componentName}}
// Purpose: {{purpose}}
// Why not DS: see docs/design_decisions.md § DS-First audit / {{componentName}}
//
// Rule 0 — this file uses only DS tokens.
// Tokens: {{tokens}}

import React from 'react';

export interface {{componentName}}Props {
  {{{propsInterface}}}
}

export function {{componentName}}({ {{propsDestructure}} }: {{componentName}}Props) {
  return (
    <div
      className="{{cssClassName}}"
      role="{{role}}"
      aria-label={ {{ariaLabel}} }
    >
      {{{body}}}
    </div>
  );
}
