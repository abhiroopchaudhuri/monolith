---
runId: {{runId}}
generated: {{generatedAt}}
owner: engineering-manager
---

# Build specs — {{briefShort}}

## File tree

```
{{fileTreeAnnotated}}
```

## Routes

| Path | Screen file | Layout parent | Data dep | Notes |
|---|---|---|---|---|
{{#each routeRows}}
| `{{this.path}}` | {{this.file}} | {{this.layout}} | {{this.data}} | {{this.notes}} |
{{/each}}

## State model

{{#each features}}
### {{this.name}}
- **Where.** {{this.where}}
- **Shape.**
  ```ts
  {{this.shape}}
  ```
- **Mutations.** {{this.mutations}}

{{/each}}

## Data contracts

{{#each fixtures}}
### `{{this.path}}`
- **Shape.** {{this.shape}}
- **Variants.** empty / error / normal — {{this.variants}}
- **Seed.** `runId`

{{/each}}

## Component decomposition

{{#each screens}}
### {{this.name}} (`{{this.file}}`)
- **Top-level** — {{this.top}}
- **Sections.**
  {{#each this.sections}}
  - `{{this.name}}` — {{this.components}}
  {{/each}}

{{/each}}

## Custom components spec

{{#if customs}}
{{#each customs}}
### `<{{this.name}}>` ({{this.folder}})

**Purpose.** {{this.purpose}}

**Why not DS.** {{this.whyNotDs}} (cite design_decisions § DS-First audit)

**Props interface.**
```ts
{{this.propsTs}}
```

**Variants.** {{this.variants}}

**A11y.** {{this.a11y}}

**Tokens used.** {{this.tokens}}

{{/each}}
{{else}}
No custom components in this run.
{{/if}}

## Build + run

```
cd out/{{runId}}/app
npm install
npm run dev     # http://localhost:{{port}}
npm run build
```
