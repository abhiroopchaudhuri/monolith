---
runId: {{runId}}
generated: {{generatedAt}}
owner: lead-designer
---

# Design decisions — {{briefShort}}

> Rule 0 — DS-First Mandate applies. Every row with a custom proposal carries a three-test proof under § DS-First audit.

## Per-section component table

| Screen | Section | Role | Component(s) | Variant intent | Alt considered | Rationale |
|---|---|---|---|---|---|---|
{{#each sectionRows}}
| {{this.screen}} | {{this.section}} | {{this.role}} | {{this.components}} | {{this.variant}} | {{this.alt}} | {{this.rationale}} |
{{/each}}

## Token applications

| Surface | Treatment | Token | Reason |
|---|---|---|---|
{{#each tokenApplications}}
| {{this.surface}} | {{this.treatment}} | `{{this.token}}` | {{this.reason}} |
{{/each}}

## State plans

{{#each statePlans}}
### {{this.screen}}
- **Empty.** {{this.empty}}
- **Error.** {{this.error}}
- **Loading.** {{this.loading}}
- **Success.** {{this.success}}

{{/each}}

## Density & breakpoints

{{#each densityRows}}
- **{{this.screen}}** — density: `{{this.density}}`; breakpoints: {{this.breakpoints}}; notes: {{this.notes}}
{{/each}}

## A11y intent

- **Landmarks.** {{landmarks}}
- **Focus order.** {{focusOrder}}
- **Announcement plan.** {{announcements}}

## DS-First audit

{{#if customProposals}}
{{#each customProposals}}
### {{this.name}} ({{this.screen}} / {{this.section}})
**Three-test proof.**
1. DS has no component for this need — {{this.test1}}
2. DS exposes no token for this property — {{this.test2}}
3. No DS pattern in docs achieves this composition — {{this.test3}}

**Outcome** — {{this.outcome}}.

{{/each}}
{{else}}
No custom proposals in this run. All UI is DS-component, DS-composition, DS-pattern, or reused-pattern.
{{/if}}
