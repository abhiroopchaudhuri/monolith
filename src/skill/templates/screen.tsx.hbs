// Auto-generated by monolith.
// Screen: {{screenId}}  Route: {{route}}  Run: {{runId}}
//
// DO NOT EDIT BY HAND. To change this file, iterate on:
//   docs/design_decisions.md + docs/build_specs.md + docs/pattern_decisions.md
// and re-run the developer agent.

import React from 'react';
{{#each imports}}
import { {{this.names}} } from '{{this.from}}';
{{/each}}
{{#each customImports}}
import { {{this.name}} } from '{{this.from}}';
{{/each}}
{{#if fixturesImport}}
import { {{fixturesImport.names}} } from '{{fixturesImport.from}}';
{{/if}}

export default function {{componentName}}() {
{{#if hooks}}
{{#each hooks}}
  {{this}};
{{/each}}
{{/if}}

  return (
    <{{rootComponent}}{{#if rootProps}} {{rootProps}}{{/if}}>
      {{#each sections}}
      {{> sectionPartial this}}
      {{/each}}
    </{{rootComponent}}>
  );
}
